# 画面設計書

## 改訂履歴

| バージョン | 日付 | 変更内容 |
|-----------|------|---------|
| 1.0.0 | 2026-02-14 | 初版作成 |
| 2.0.0 | 2026-02-25 | レビュー指摘反映: 受験日設定のUI導線追加、削除機能のMVP化、エラー表示方針の明確化、サマリー情報を要件に追記 |
| 3.0.0 | 2026-04-25 | Claudeが書いた設計書をベースに、自分自身の手で書き換える |

---

## 1. 画面一覧

| 画面ID | 画面名 | パス | 説明 | MVP |
|--------|--------|------|------|-----|
| HOME | ホーム画面 | `/` | 記録入力とカウントダウン表示 | ✅ |
| RECORDS | 記録一覧画面 | `/records` | 過去の記録を一覧表示・削除 | ✅ |

---

## 2. 画面遷移図

```
┌─────────────┐
│    HOME     │ ← 初期表示
│   (ホーム)   │
└──────┬──────┘
       │
       │ 「記録を見る」ボタン
       ↓
┌─────────────┐
│  RECORDS    │
│ (記録一覧)   │
└──────┬──────┘
       │
       │ 「戻る」ボタン / ブラウザバック
       ↓
┌─────────────┐
│    HOME     │
└─────────────┘
```

**遷移ルール**:
- 初期表示は常にHOME画面
- 各画面からHOMEに戻れる
- ブラウザバックで前の画面に戻る

---

## 3. HOME画面(ホーム)

### 3.1 画面レイアウト(モバイル)

```
┌──────────────────────────────┐
│                              │
│    📅 試験まであと 65日  [✏️] │  ← カウントダウン(タップで編集)
│                              │
├──────────────────────────────┤
│                              │
│   📝 今日の記録を入力         │  ← セクションタイトル
│                              │
│   実施日                      │
│   ┌────────────────────────┐ │
│   │ 2026-02-14        [📅] │ │  ← 日付入力
│   └────────────────────────┘ │
│                              │
│   解いた問題数               │
│   ┌────────────────────────┐ │
│   │ (例: 80)               │ │  ← 数値入力(placeholder)
│   └────────────────────────┘ │
│                              │
│   正答した問題数             │
│   ┌────────────────────────┐ │
│   │ (例: 60)               │ │  ← 数値入力(placeholder)
│   └────────────────────────┘ │
│                              │
│   正答率: 84.0%              │  ← 自動計算(リアルタイム)
│                              │
│   ┌────────────────────────┐ │
│   │     📊 記録する         │ │  ← 記録ボタン
│   └────────────────────────┘ │
│                              │
├──────────────────────────────┤
│                              │
│   前回比: +8.0% ⬆           │  ← 前回比表示(記録後)
│                              │
├──────────────────────────────┤
│                              │
│   ┌────────────────────────┐ │
│   │   📋 記録を見る         │ │  ← 遷移ボタン
│   └────────────────────────┘ │
│                              │
└──────────────────────────────┘
```

### 3.2 構成要素

#### A. カウントダウンエリア（受験日設定を兼ねる）

**受験日が設定済みの場合**:
```
┌──────────────────────────────┐
│  📅 試験まであと 65日    [✏️] │  ← 編集アイコンをタップで編集
└──────────────────────────────┘
```

**受験日が未設定の場合**:
```
┌──────────────────────────────┐
│  📅 受験日を設定する →        │  ← タップで入力欄を表示
└──────────────────────────────┘
```

**受験日設定UI（インライン展開）**:

カウントダウンエリアをタップ（または編集アイコンをタップ）すると、同じ場所にインラインで日付入力欄が展開される:

```
┌──────────────────────────────┐
│  📅 受験予定日               │
│  ┌────────────────────────┐ │
│  │ 2026-04-20        [📅] │ │  ← 日付ピッカー
│  └────────────────────────┘ │
│  ┌──────┐  ┌──────────┐    │
│  │ 保存  │  │ キャンセル │    │
│  └──────┘  └──────────┘    │
└──────────────────────────────┘
```

**設計判断**: 別ページ（/settings）ではなくインライン展開にした理由は、画面遷移を減らし、操作のハードルを下げるため。将来的に設定項目が増えた場合は独立した設定画面を検討する。

#### B. 記録入力フォーム

**フィールド一覧**:

| フィールド | 入力タイプ | デフォルト値 | placeholder | 必須 |
|-----------|-----------|-------------|-------------|------|
| 実施日 | date | 今日 | - | ✅ |
| 解いた問題数 | number | 空(未入力) | 例: 80 | ✅ |
| 正答した問題数 | number | 空(未入力) | 例: 60 | ✅ |

**バリデーション**:
- 解いた問題数: 1以上200以下の整数
- 正答した問題数: 0以上、解いた問題数以下の整数
- エラーはフィールド直下にインライン表示（赤文字）
- 該当フィールドを赤枠でハイライト

**正答率の表示**:
- 入力中にリアルタイムで計算・表示
- 小数点第1位まで表示
- 両方の数値が入力されるまで「---%」と表示

#### C. 前回比表示エリア

```
前回比: +8.0% ⬆   → 緑色
前回比: -3.5% ⬇   → 赤色
前回比: ±0.0% →   → グレー
```

**表示タイミング**: 記録ボタン押下後にフェードインで表示
**前回の定義**: 日付が最新の記録（同日複数の場合はcreatedAtで判定）

#### D. 遷移ボタン
- タップで記録一覧画面へ遷移

### 3.3 インタラクション

#### 記録追加フロー
```
1. ユーザーがフォームに入力
   ↓
2. 正答率がリアルタイムで計算・表示
   ↓
3. 「記録する」ボタンをタップ
   ↓
4. バリデーション実行
   ↓ (成功)
5. 保存前に前回の記録を取得（前回比計算のため）
   ↓
6. LocalStorageに保存
   ↓
7. 前回比を計算・表示
   ↓
8. フォームをクリア（日付は今日のまま、数値は空に）
   ↓
9. トースト通知「記録を追加しました ✓」（2秒で自動消去）
```

#### エラー時の表示

```
解いた問題数
┌────────────────────────────┐  ← 赤枠
│ -5                         │
└────────────────────────────┘
⚠️ 1以上の数値を入力してください
```

- エラーメッセージはフィールド直下にインライン表示
- 赤枠 + 赤文字
- リアルタイムバリデーション（入力変更時にエラーを更新）

---

## 4. RECORDS画面(記録一覧)

### 4.1 画面レイアウト(モバイル)

```
┌──────────────────────────────┐
│  ← 戻る      📊 記録一覧      │  ← ヘッダー
├──────────────────────────────┤
│                              │
│  📈 平均正答率: 81.3%         │  ← サマリー情報
│  📝 総演習回数: 15回          │
│                              │
├──────────────────────────────┤
│                              │
│  ┌──────────────────────────┐│
│  │ 2026-02-16          [🗑] ││  ← 削除ボタン
│  │ 80問中 68問正解          ││
│  │ 正答率: 85.0%            ││
│  │ 前回比: +1.0% ⬆          ││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │ 2026-02-15          [🗑] ││
│  │ 50問中 42問正解          ││
│  │ 正答率: 84.0%            ││
│  │ 前回比: +8.0% ⬆          ││
│  └──────────────────────────┘│
│                              │
│  ┌──────────────────────────┐│
│  │ 2026-02-14          [🗑] ││
│  │ 50問中 38問正解          ││
│  │ 正答率: 76.0%            ││
│  │ 前回比: --               ││  ← 最初の記録
│  └──────────────────────────┘│
│                              │
│  ... (スクロールで続く)       │
│                              │
└──────────────────────────────┘
```

### 4.2 構成要素

#### A. ヘッダー
- 左: 戻るボタン(HOME画面へ)
- 中央: タイトル「記録一覧」

#### B. サマリー情報
```
📈 平均正答率: 81.3%
📝 総演習回数: 15回
```

**備考**: 要件定義書のMVP機能には明記されていないが、計算コストが低く有用なため含める。

#### C. 記録カード(1件)
```
┌──────────────────────────────┐
│  2026-02-16             [🗑] │  ← 日付 + 削除ボタン
│  80問中 68問正解              │
│  正答率: 85.0%                │
│  前回比: +1.0% ⬆              │
└──────────────────────────────┘
```

**表示ルール**:
- 日付降順（同日はcreatedAt降順）
- 最も古い記録は前回比なし（「--」と表示）
- 前回比の計算方法は03_database.md セクション5を参照

#### D. 削除機能

**削除ボタン**: 各カードの右上にゴミ箱アイコン(🗑)

**削除確認ダイアログ**:
```
┌──────────────────────────────┐
│                              │
│   この記録を削除しますか？    │
│                              │
│   2026-02-16                 │
│   80問中 68問正解             │
│   正答率: 85.0%               │
│                              │
│  ┌──────────┐ ┌──────────┐  │
│  │ キャンセル │ │   削除    │  │
│  └──────────┘ └──────────┘  │
│                              │
└──────────────────────────────┘
```

- 「削除」ボタンは赤色
- 削除後はカードが消え、サマリー情報も更新される
- 削除後にトースト通知「記録を削除しました」
- 削除によって隣接レコードの前回比が変わる場合は再計算される

### 4.3 状態別の表示

#### 記録が0件の場合
```
┌──────────────────────────────┐
│                              │
│       📝                     │
│   まだ記録がありません        │
│                              │
│   ホーム画面から              │
│   記録を追加してください      │
│                              │
│   ┌────────────────────────┐ │
│   │    ホームに戻る          │ │
│   └────────────────────────┘ │
│                              │
└──────────────────────────────┘
```

---

## 5. 共通UI要素

### 5.1  色使い

| 用途 | 色 | 用途 |
|------|--------|--------|
| プライマリボタン | 青 | 記録する、保存 |
| セカンダリボタン | 白 | 記録を見る、戻る、キャンセル |
| 成功(向上) | 緑 | 前回比の上昇、成功 |
| 警告(低下) | 赤 | 前回比の下降、削除ボタン、エラー表示 |
| 中立 | グレー | 前回比±0 |

### 5.2 トースト通知
- 画面下部からスライドイン、2秒で自動消去
- 成功: 背景 緑
- 削除: 背景 グレー

---

## 6. アニメーション・フィードバック

| 対象 | アニメーション |
|------|-------------|
| ボタンタップ | 背景色を暗く |
| 前回比表示 | フェードイン |
| トースト通知 | 画面下部からスライドイン |

---

## 7. アクセシビリティ

### 7.1 フォーム
- 各入力フィールドに`<label>`を設定
- エラーメッセージを`aria-live="polite"`で通知
- タブキーでの移動順序を適切に設定
- 削除確認ダイアログに`role="alertdialog"`を設定

---